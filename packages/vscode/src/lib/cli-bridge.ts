/**
 * packages/vscode/src/lib/cli-bridge.ts
 *
 * Legacy placeholder for commands that used to delegate through @devory/cli.
 * The extension no longer uses the CLI bridge for task mutation or run start.
 */
export {};
